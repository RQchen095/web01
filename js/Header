function loadHeader() {
    const header = document.getElementById('navheader');
    const navheader = `
    <nav>
        <a href="https://www.pu.edu.tw/">靜宜大學</a>
        <a href="index.html">這是首頁</a>
    </nav>
    `;
    if (header) {
        header.innerHTML = navheader;
    }
}
loadHeader();